# Versions are lower bounds, not hard pins, so pip can resolve a set that is
# mutually compatible and has Python 3.13 wheels.

# --- Web API ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9          # lets FastAPI accept file uploads
pydantic>=2.9
pydantic-settings>=2.5

# --- RAG pipeline ---
langchain-core>=0.3.29
langchain-text-splitters>=0.3
langchain-community>=0.3.14      # PyPDFLoader (PDF text extraction)
langchain-chroma>=0.2.1          # ChromaDB integration; pulls a compatible chromadb
langchain-huggingface>=0.1.2     # wrapper for the local embedding model
langchain-ollama>=0.2            # local LLM via Ollama (pulls the `ollama` client)
sentence-transformers>=3.3       # runs the local embedding model
chromadb>=0.5.20                 # vector store; exact version resolved with langchain-chroma
pypdf>=5.0                       # PDF parsing used by PyPDFLoader

# --- Database (document metadata) ---
sqlalchemy>=2.0
psycopg[binary]>=3.1            # PostgreSQL driver for the postgresql+psycopg:// URL

# --- Tests ---
pytest>=8.3
httpx>=0.27                      # used by FastAPI's TestClient
